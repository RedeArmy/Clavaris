package com.clavaris.identity.application.usecase.resolveredirecturl;

/**
 * Which of a {@code RedirectPolicy}'s two URL pairs applies — sign-in completion or sign-up
 * completion.
 */
public enum RedirectAction {
  SIGN_IN,
  SIGN_UP
}
