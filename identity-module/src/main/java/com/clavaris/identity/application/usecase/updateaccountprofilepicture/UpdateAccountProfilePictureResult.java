package com.clavaris.identity.application.usecase.updateaccountprofilepicture;

/** The new picture's own storage key — mostly useful for tests; the controller never needs it. */
public record UpdateAccountProfilePictureResult(String storageKey) {}
