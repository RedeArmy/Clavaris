# Roadmap and Release Plan — Clavaris

🟡 En revisión

## 1. Structure

Three releases, sequenced by what unblocks a real consumer first, not by module boundary.

## 2. v1 — Core identity + OIDC (unblocks JobSeeker's Wave 1)

**Spike complete (2026-08-17): GO** — the Spring Authorization Server spike required by ADR-0003's addendum (per-Organization issuer resolution, per-tenant `JWKSource`, tenant-scoped `RegisteredClientRepository`) validated ADR-0010 §5 end-to-end against real code (discovery, JWKS isolation, client-registry isolation, all confirmed with real HTTP calls and real signature verification — see ADR-0003's addendum for full results). Two small, targeted implementation patterns are required (a custom discovery filter; explicitly wiring `JWKSource` into `HttpSecurity`'s shared-object registry) — neither is a fight against the framework. The rows below can now be built for real, using the spike's findings as implementation guidance for the first `identity-module`/`client-registry-module` use cases.

**Status column added 2026-08-24** (an SDE-III review found the table below, unchanged since 2026-08-17, gave no visual signal that 5 of 8 rows are fully shipped and 3 have zero code behind them — someone skimming it would reasonably conclude v1 is nearly done, when the honest read is "the hardest, highest-risk rows are done; three real features remain unstarted").

| Capability | Module | Status |
|---|---|---|
| Registration, password login, email verification, password recovery | `identity-module` | ✅ Shipped |
| Refresh token issuance + rotation with reuse detection | `identity-module` | ✅ Shipped |
| Social login (Google, GitHub) | `identity-module` | ⛔ Not started — zero `SocialIdentity`/OAuth-client-registration code exists |
| Authorization Code flow + PKCE, discovery/JWKS/userinfo/revoke/end-session | `client-registry-module` | ✅ Shipped |
| `Organization` tenant isolation (operator-created, isolated account pool per consuming system) | `organization-module` | ✅ Shipped |
| Basic workspaces (create, add member, roles, remove member) — ADR-0010 §3 addendum | `organization-module` | ✅ Shipped 2026-08-27 — `security-architecture.md` §7b, live-verified (`WorkspaceIntegrationTest`). **v1 scope narrowed by explicit decision**, not built exactly as this row originally described: two-role membership (`ADMIN`/`MEMBER`, no `OWNER`) and direct member provisioning (no invitation) — see ADR-0010 §3's addendum and `business-rules.md` BR-WS-01/04/05. Also closed a real, previously-open gap: `WorkspaceMembership` erasure on `Account`/`Organization` deletion (ADR-0007's own documented obligation, `security-architecture.md` §7 step 2 / §7a) |
| Per-Organization issuer/JWKS (path-based `{clavarisBaseUrl}/o/{organizationId}`) + manually-triggered, audited key rotation (ADR-0010 §5) | `identity-module` | ✅ Shipped, including rotation-with-overlap (TD-SEC-008) |
| Two-layer rate limiting: fixed anti-abuse threshold (never tenant-configurable) + operator-managed per-Organization capacity ceiling (ADR-0010 §6) | `identity-module` / `organization-module` | ✅ Shipped, including Redis-fail-open behavior (TD-SEC-022) |
| Admin account-deletion API (consumed by JobSeeker's own ADR-0013 cascade) | `identity-module` | ✅ Shipped 2026-08-26 — `security-architecture.md` §7, live-verified (`DeleteAccountIntegrationTest`) |
| Admin organization-deletion API (BR-DATA-02/03's own organization-level equivalent) | `organization-module` | ✅ Shipped 2026-08-26 — `security-architecture.md` §7a, live-verified (`DeleteOrganizationIntegrationTest`). Not originally in this table — genuinely new scope, zero code existed until requested; discovered along the way that `accounts`/`oauth_clients`/`signing_keys` never had real database-level FKs to `organizations` either (`data-model.md` §2 now corrected) |

**Exit criterion:** JobSeeker completes a real login → token → `/userinfo` round trip against a deployed Clavaris instance, and the mandatory external security review has no open critical/high findings.

**Added 2026-08-25, resolved same day (TD-SEC-028):** the table row above listed `/userinfo`/`end-session` as "✅ Shipped" alongside the rest of the Authorization Code flow, but neither had ever been invoked by a test — a real, previously-unquestioned assumption that Spring Authorization Server's own defaults just worked. They didn't: `/userinfo` always returned 401 for a valid token (no `JwtDecoder` existed anywhere to authenticate the Bearer credential against), and `end_session_endpoint` 404'd unconditionally (outside this chain's own `securityMatcher`) despite the discovery document confidently advertising it. Both fixed and live-verified end to end (`technical-debt-register.md` §6) — this exit criterion's own `/userinfo` round trip is now something that has actually happened, not just something the row claims will work.

**Added 2026-08-25 (SDE-III review, §12 below), resolved same day:** the CLAUDE.md §2 success metric "passes the OpenID Foundation conformance test suite" had a real, previously-undocumented asterisk — `technical-debt-register.md` TD-SEC-026 found that Spring Authorization Server's consent screen structurally never rendered (`requireAuthorizationConsent` was never set to `true` anywhere). **Closed 2026-08-25, ADR-0017**: consent is now a real, per-client, secure-by-default `OAuthClient` attribute — JobSeeker's own registration takes the explicit `requireConsent: false` opt-out (a trusted first-party consumer, no user-facing consent decision needed for it today), while any future, less-trusted consumer's clients get a real, live-verified consent screen by default. The conformance suite now has a real flow to exercise either way, rather than a structurally-dormant page.

**Gate items — updated 2026-08-24, both original items now closed, and a second pass closed the rest.** The 2026-08-23 rate-limiting review named two P1 gate items (`technical-debt-register.md` §5 item 8, TD-SEC-022/TD-TEST-003) that should close before the external security review is scheduled — **both closed same-day.** A separate, later pass the same day (§5 item 8's own note, TD-ARCH-002/TD-SEC-019) closed the two remaining P1 rows that existed anywhere in the register at that point. **As of 2026-08-24: zero P1 rows are open anywhere in `technical-debt-register.md` §2.** The external security review named in this section's own exit criterion can now genuinely be scheduled — see `technical-debt-register.md` §5 item 10. This does not mean v1 itself is done (see the table's Status column above) — it means the security-review *gate* specifically is clear, independent of whether every v1 capability row has shipped yet.

**Updated 2026-08-26**: admin account-deletion shipped (table row above) — 6 of 8 v1 capability rows are now genuinely done, live-verified, not just decided. Social login and `Workspace`/basic workspaces remain the only two rows with zero code behind them.

**Updated 2026-08-27**: Workspace shipped (table row above, v1 scope narrowed by explicit decision — see ADR-0010 §3's addendum). Social login is now the only v1 capability row with zero code behind it — 9 of 10 rows in this table are genuinely done, live-verified.

## 3. v1.1 — Developer experience + hardening

| Capability | Why deferred, not dropped |
|---|---|
| Self-service client registration console | v1 clients are registered manually — acceptable at one-consumer scale, not at "reusable across projects" scale |
| Self-service `RateLimitPolicy` tuning (tenant-facing) + scheduled/unattended signing-key rotation | ADR-0010 §5.2/§6.2 — v1 ships operator-managed capacity ceilings and manually-triggered rotation on purpose (small operator-managed tenant count); self-service capacity tuning needs audit logging shipped first, and unattended rotation needs a real scheduler, neither of which is v1 scope |
| MFA (TOTP) | Real security improvement, not required for JobSeeker's own threat model at launch — see `docs/01-product/prd-mvp.md` §2.1 |
| Session management UI polish | v1 ships a functional, not polished, version |
| Webhooks (`webhook-module`) | 🟡 Proposed ADR-0007 — Clerk-style signed event delivery; needed once a second consumer exists to react to, not blocking JobSeeker's own login integration |
| Passkeys, breached-password check, new-device step-up MFA, user impersonation | Sourced from `docs/00-vision/clerk-feature-analysis.md` §6/§7 — real gaps against a mature comparable system, none of them launch-blocking for JobSeeker's Wave 1 |
| ~~Rate-limit observability (per-rule/per-endpoint allow/block metrics and alerting)~~ | **Shipped 2026-08-24 — `technical-debt-register.md` TD-FUT-011 closed.** Real Micrometer metrics at every named call site, `docker-compose.observability.yml` (Prometheus/Alertmanager/Grafana/Zipkin/Mailpit), 5 real alert rules with real email delivery, and full per-request tracing (a scope expansion beyond ADR-0015's own original text) — all live-verified against a real running stack, not just configured. |
| Embedded/branded login (iframe-modal + per-client custom domain + `ClientBranding`) | 🟡 Proposed ADR-0009 — real infra prerequisite (dynamic TLS, DNS verification); the plain full-page redirect login (already v1 scope) works with zero setup in the meantime, so this is additive, not blocking |
| Self-registration + approval for a consuming application's own public end users (candidates/recruiters), tenant-optional | `technical-debt-register.md` TD-FUT-019 — separate from Workspace (BR-WS-04, internal admin teams only), scoped out explicitly on 2026-08-27 rather than conflated with it; real open design questions (who approves, what new status) not yet resolved |

## 4. v2 — Enterprise-shaped features (no committed date)

SAML, SCIM provisioning, configurable per-organization roles, multi-region deployment. None of these are needed by any current or near-term consumer — they stay on this list as acknowledged future scope, not as commitments, per `project-charter.md` §3.

## 5. What is deliberately not on this roadmap (v1/v1.1/v2)

A commercial multi-tenant offering, a hosted/managed version of Clavaris for third parties, a general password-manager or authenticator-app product. See `vision-document.md` §5 — these are non-goals *for the releases scheduled here*, not a claim about forever: `vision-document.md` §7 records a declared long-term intent to eventually take Clavaris to market, but that has no committed date and does not add scope to v1/v1.1/v2 — it only informs *which* architectural investments (e.g. ADR-0010's tenant isolation shape) are worth making early. Also explicitly not on this roadmap, ever *for a consumer's own product concerns*: billing/subscription management, feature/plan gating, "waitlist" launch-gating for a consuming application — product-specific business logic that belongs in each consumer's own backend, not here. (Whether Clavaris someday needs its *own* billing for its *own* tenant customers, if commercialized, is a separate future question `vision-document.md` §7 does not resolve either.)

## 6. Sequencing dependency on JobSeeker

Clavaris's v1 exists specifically to unblock JobSeeker's Wave 1 identity integration (`../JobSeeker/docs/01-product/roadmap-and-release-plan.md` §3). This is the one hard external deadline pressure on an otherwise self-paced roadmap.

## 7. Open risk — uncoordinated roadmaps (not resolved here)

JobSeeker's roadmap and this one are maintained in two separate repositories with no shared tracking mechanism. If Clavaris's v1 slips, JobSeeker's Wave 1 has no documented fallback (`project-charter.md` §7 names this same gap from Clavaris's side). Resolving this requires an explicit decision — build a throwaway minimal auth in JobSeeker to unblock it, or accept the JobSeeker delay — that has not been made. Flagging it here so it isn't silently forgotten.

## 8. Open risk — no explicit v1 cut list (partially resolved)

Every row in §2's table currently reads as equally required. For a solo developer pulled by an external deadline (§6), that's a risk in itself: if time runs short, there is no pre-agreed answer to "what ships late vs. what ships thin."

**One specific instance of this question is now decided, not open:** whether full per-`Organization` isolation (JWKS, issuer, two-layer rate limiting — ADR-0010 §5/§6) needs to be *working* to unblock JobSeeker, or could ship as "data model ready, protocol mechanics deferred." **Decided: build it in full from the start, deliberately, even though it is not strictly required for JobSeeker's login flow alone.** Reasoning (user decision, 2026-08-17): the `iss` claim is embedded in every issued token and pinned by every verifier's config — adding per-organization issuance *after* JobSeeker has already integrated against a shared-issuer v1 would be a breaking migration for an already-live consumer. Paying the extra up-front implementation time now, while zero accounts and zero integrated consumers exist, is accepted as cheaper than refactoring later. This is consistent with, and reinforces, the reasoning already in ADR-0010 §5.1.

**Consequence worth naming explicitly:** this removes the fallback option of shipping a simpler v1 if the Spring Authorization Server spike (ADR-0003 addendum) turns out harder than expected — the full tenant-isolation mechanism is no longer an optional add-on to a working simple baseline, it *is* the v1 baseline. This raises the cost of the spike failing and is the reason the spike should run immediately, before further design work, rather than later in the schedule.

The broader question (an explicit cut list for *every other* row in §2, not just this one) remains open — not resolved here, flagged so any further cuts are a deliberate decision, not a panic decision mid-implementation.

## 9. ~~Open risk — onboarding docs describe a state that stopped being true weeks ago~~ (resolved 2026-08-24, same day it was named)

`README.md` and this project's own top-level engineering-context notes described this project as documentation-first with zero domain/application code — a fair description of 2026-08-17, actively false by the time this row was written. Both rewritten same-day to describe the actual current state; `technical-debt-register.md` TD-PROC-004 has the full closure detail.

## 10. ~~Open risk — public/SPA OAuth client support is an undecided integration question~~ (resolved 2026-08-24, same day it was named)

Whether a consumer's frontend may ever call Clavaris's token endpoint directly from browser JavaScript (a public/SPA client, needing a real CORS policy) or must always go through its own backend (a confidential client, same-origin token exchange) had never been decided or documented. **Decided: confidential clients only, v1 and for the foreseeable future — see ADR-0013.** The investigation behind that decision found every layer (DB schema, domain model, `RegisterOAuthClientService`, both `RegisteredClientRepository` adapters) already assumed a confidential client; this was an unstated conclusion, not a genuine open question, formalized once someone actually looked. Consequence for this roadmap's own §2 exit criterion (JobSeeker completing a real login round trip): JobSeeker's `auth-module` is already a real backend per `integration-design.md` §3, so this decision doesn't block it — but it does mean any future consumer without its own backend cannot integrate with Clavaris as designed today, a real constraint worth knowing before, not after, that consumer is scheduled. `technical-debt-register.md` TD-FUT-015 has the full closure detail.

## 11. ~~Open question — should Clavaris pursue ISO 27001/SOC 2 certification, and is it feasible at this stage?~~ (resolved 2026-08-24, same day it was raised)

Whether login/identity being the core of this product meant ISO certification should be an active goal now had never been evaluated against real cost/timeline data or market precedent. **Decided: build toward ISO 27001 + SOC 2 Type II *readiness* in parallel, defer the paid certification audit to a real customer's procurement process — see ADR-0016.** Investigated live, not assumed: ISO 27001 costs $25k–$40k and 200–800+ internal hours over 6–12 months even for a small funded startup — wildly disproportionate to a solo-developer, pre-launch project. Checked what this project's own stated long-term model (Clerk) actually holds: SOC 2 Type II, not ISO 27001, relying on its cloud providers' own certification instead — the realistic precedent for where this project is today, not the more mature Auth0's (which holds both). **ISO/IEC 29115 adopted immediately as code, not deferred with the rest** — every ID token the interactive flow issues now carries `acr`/`amr` claims reflecting the one real authentication method this codebase has today (password, ISO/IEC 29115 Level 2), verified end-to-end against a real signed token. The gap-analysis this decision required also caught a real documentation-accuracy bug (`security-architecture.md` §7 described the unimplemented admin account-deletion API as though it existed) — corrected same-day. `technical-debt-register.md` TD-PROC-006 and TD-FUT-016 have the full detail; TD-FUT-016 specifically is the concrete list of organizational-control gaps (risk register, general incident-response plan, vendor management, access reviews, asset inventory) "readiness" still requires beyond the code-level controls this project already has. **Update 2026-08-25 (§12 below): TD-FUT-016 itself closed 2026-08-24, the same week this row was written** — all five documents it named now exist.

## 12. SDE-III review, 2026-08-25 — strengths/weaknesses since the last one (2026-08-24), and this roadmap's own staleness

A full week's worth of real shipping happened between the 2026-08-24 review (§9–§11 above) and this one — not a quiet week: TD-FUT-011 (observability/alerting, fully implemented and live-verified, not just decided), TD-FUT-016 (the five ISO/SOC2-readiness governance docs, all written), TD-TEST-002/TD-TEST-004 (event_outbox retention, a real load test that found a genuine, previously-unknown capacity ceiling — TD-FUT-017), TD-SEC-009 (a real Content-Security-Policy, live-verified), TD-SEC-027 (a real Dependabot CI failure diagnosed and fixed, including a real self-caught bug in the first fix attempt). This section itself — §3's own TD-FUT-011 row and §11's own TD-FUT-016 reference — was found stale by this same review pass and corrected above, the same "roadmap describes a state that stopped being true" pattern TD-PROC-004 already closed once for the onboarding docs. Tracked as **TD-PROC-007** (`technical-debt-register.md`), closed same-day.

**Strengths, verified fresh, not carried forward from memory:** a clean `mvn clean verify` across all 5 modules; **97.3% aggregate line-instruction coverage** (`app/target/site/jacoco-aggregate/jacoco.xml`, the cross-module report — not the lower-looking per-module numbers, which undercount by design since integration tests for one module's adapters often live in `app`); zero `TODO`/`FIXME`/`XXX` anywhere under any module's `src/main`, still true; zero P0/P1 rows open anywhere in the technical debt register, still true; the observability work this week caught 3 real bugs (a Grafana mount conflict, two wrong Zipkin property names) entirely through live verification against a real running stack rather than config review, and the Dependabot CI fix caught a real `github.actor`-vs-PR-author bug on its own first live attempt — the "confirmed live, not assumed" discipline this register has followed all along keeps finding real things a config-only review wouldn't have.

**Weaknesses, real ones, not manufactured to fill a section:**
- **This document's own staleness recurring** (TD-PROC-007) is itself the clearest evidence of the solo-developer bus-factor risk `risk-register.md` already names — a fast-shipping week outpaced the one process (updating this roadmap) that has no second person to catch it drifting. Same root cause as TD-PROC-004/005/006, not a new class of problem, but its third occurrence in nine days is worth naming as a pattern, not three unrelated incidents.
- **TD-SEC-026** (consent screen never renders) sat in the debt register for a full day without anyone (including this review, until now) connecting it back to this roadmap's own OIDC-conformance success metric (CLAUDE.md §2) — cross-referenced same-day this review found it (§2 above), and **closed the same day, ADR-0017**: per-client, secure-by-default consent, live-verified end to end (`technical-debt-register.md` §6). The gap this bullet originally named — the connective tissue between two documents each independently correct but not cross-referenced — is the more durable lesson than the underlying finding itself, which closed quickly once named.
- **TD-FUT-017** (the Argon2id concurrency ceiling the load test found) has real alerting watching it (`TokenIssuanceLatencyHigh`, TD-FUT-011) but still no decision on what to *do* when it fires — more cores, tuned Argon2 cost parameters, or accepting the anti-abuse rate limit as the de facto capacity control. Genuinely blocked on TD-FUT-013 (no production host exists yet to size), not neglect, but worth naming as the next real decision once that host exists.
- **Operational surface for one person keeps growing.** Four Compose files now exist (`docker-compose.yml` plus three optional overlays — Infisical, observability, override), the observability overlay alone adding 5 more containers. Each addition was individually justified and each is genuinely optional (never required for `docker compose up`'s own default flow), but the *sum* is a real, worth-naming trend for a project whose own `nfr-quality-attributes.md` §6 names solo-developer operability as a hard constraint, not an aspiration — not yet at a point requiring action, but worth checking again at the next review rather than only ever counting one addition at a time.

**Points worth implementing next, unprioritized by size, ordered by how directly each blocks something else already committed:**
1. ~~**Decide TD-SEC-026**~~ — **done, same day, ADR-0017.** Per-client, secure-by-default consent; JobSeeker's own registration opts out explicitly.
2. **TD-FUT-013** (production deployment artifact) — named as the next real infrastructure investment back on 2026-08-24 and still true; now additionally the prerequisite for resolving TD-FUT-017's own open question (what host, how many cores).
3. ~~The three genuinely unbuilt v1 capabilities (social login, `Workspace`, admin account-deletion)~~ — **admin account-deletion (and its own organization-level equivalent, genuinely new scope) both shipped 2026-08-26, live-verified — see §2's table above.** Social login and `Workspace` remain exactly what this bullet originally named them: unstarted, zero code.

## 13. SDE-III review, 2026-08-26 — a full week's worth of shipping since §12, whole-codebase scope

Requested explicitly as a deep, whole-codebase pass, not a re-check of the last several turns' own work. Confirms this session's own account/organization hard-delete work (§2's table, `security-architecture.md` §7/§7a) is real, live-verified, and 100%-covered by JaCoCo — and surfaces what asking "what does this new capability change about the rest of the system" turns up that building the capability itself didn't.

**Strengths, verified fresh:** a clean `mvn clean verify` across all 5 modules on current `master`; **97.40% aggregate instruction coverage** (`app/target/site/jacoco-aggregate/jacoco.csv`, recomputed directly from the CSV, not eyeballed) — effectively flat against last week's 97.3%, meaning a full week of new destructive-operation work landed without eroding the bar; zero `TODO`/`FIXME`/`XXX` anywhere under any module's `src/main`, still true; every one of the four new deletion-package classes (`DeleteAccountService`, `DeleteOrganizationService`, both controllers) individually confirmed at 100% coverage, not just folded into the aggregate. Three PRs shipped and merged this week (#60/#61/#62) — runbook accuracy, audit-coverage proof, and the full account+organization hard-delete surface — each with the same "confirmed live, not assumed" discipline: real Postgres, real Authorization Code exchanges, real cascade assertions, not mocks.

**Weaknesses, found by deliberately looking past what shipped, not at it:**
- **TD-SEC-031** (new, P2): tracing every "revoke everything for this account" call site end to end (not just reading each service's own Javadoc claim) found that none of them — password reset, refresh-token reuse detection, or either new deletion service — invalidate a live `HttpSession`. The `PlatformAccount` tier already solved this correctly (`PlatformAccountSessionRevokerBridge`); the tenant `Account` tier structurally has the same Redis-indexed-session capability available and simply never wired it in. A narrow window (bounded by the 30-minute session TTL, and requiring an already-live session at the exact moment of revocation), but a real one, and the kind of gap that's invisible from inside any single feature's own test suite — DeleteAccountIntegrationTest and DeleteOrganizationIntegrationTest both correctly prove what they set out to prove; neither was ever asked whether a stale session survives.
- **TD-SEC-030** (new, P2): the admin API's own destructiveness grew this week (organization hard-delete is explicitly documented, in its own Javadoc, as "the single most destructive operation this management API exposes") without a matching look at what protects the surface it lives on. Zero rate limiting exists anywhere on `/api/v1/admin/**` — confirmed by grep, not inference. ADR-0010 §6's two-layer design was built for the login/token/dashboard surfaces and was never extended here, a gap that predates this week's work but only became consequential once this week's own capability made the admin API's worst case meaningfully worse.
- **TD-ARCH-007** (new, P3, low urgency): `DeleteOrganizationService` and `DeleteAccountService` are structurally parallel services except one writes an outbox event and the other doesn't — currently inert (`webhook-module` doesn't exist), but a real, avoidable inconsistency between two pieces of code built days apart by the same hand, exactly the kind of drift a fresh review pass exists to catch before it compounds.
- **TD-PROC-002 keeps recurring as a pattern, not a single finding.** Every core engineering/product doc is still "🟡 En revisión," unchanged since 2026-08-20 — now six days and at least four intervening SDE-III review passes without anyone (including those passes) acting on it. Worth naming plainly: a review that finds new P2 security gaps every time it looks but never budgets time to close the one process row asking for a documentation status pass is itself a small instance of the same "the fast-shipping week outpaces the process with no second person to catch it" pattern §12 already named for TD-PROC-007.

**Points worth implementing next (as of 2026-08-26, before same-day closure below):**
1. ~~**TD-SEC-031** — small, has a working pattern to copy (`PlatformAccountSessionRevokerBridge`), no design decision needed. Cheapest of the three new findings to close.~~ **Closed same day — see the follow-up entry below.**
2. ~~**TD-SEC-030** — needs one real design decision first...~~ **Closed same day — see below.**
3. **TD-FUT-013** (production deployment artifact) — unchanged from §12, still the next real infrastructure investment, still blocking TD-FUT-017's own open question.
4. Social login and `Workspace` — still the two v1 capability rows with zero code. Unstarted, not forgotten.

### 2026-08-26 follow-up — TD-SEC-030/031/TD-ARCH-007 implemented and closed same day

Requested explicitly as full, real implementations — "totalmente completos y reales," not designs deferred to a later pass. All three closed; full detail in `technical-debt-register.md` §6, summarized here for the roadmap's own record.

**TD-SEC-031** (tenant `Account` session revocation): new `AccountSessionRevoker` port/bridge, mirroring `PlatformAccountSessionRevoker` exactly, wired into all four revocation call sites (`ConfirmPasswordResetService`, `RotateRefreshTokenService`'s reuse cascade, `DeleteAccountService`, and `DeleteOrganizationService`'s own organization-wide erasure path). **Caught and fixed two real regressions before this was actually done, not one**: the fix required adding `ConcurrentSessionFilter` wiring to `OrganizationAuthorizationServerConfig`, which had none before. The first version of that wiring broke `DeleteAccountIntegrationTest` outright (Spring Security's own default expired-session handling terminates the request even on the chain's own `permitAll()` login page, leaving a browser with a stale cookie unable to log back in at all) — fixed with a custom `SessionInformationExpiredStrategy` that invalidates the stale session and lets the request continue instead of terminating it. That fix then broke a *second*, unrelated thing, caught only once the *full* `app` suite ran, not just the two deletion tests: `RefreshTokenRotationIntegrationTest`'s own reuse-cascade check started getting `302` instead of `400`. Root-caused with `javap -c` bytecode disassembly of `ConcurrentSessionFilter` plus real Spring Security `TRACE` logging (not guessed): `ConcurrentSessionFilter`'s own `doLogout()` step clears `SecurityContextHolder` unconditionally, and on this chain's actual filter order SAS's `OAuth2TokenEndpointFilter` sits *after* the generic `.anyRequest().authenticated()` check — so a stale, unrelated browser session riding along on an otherwise-valid `/oauth2/token` refresh-token exchange (Basic-Auth client credentials, nothing to do with that session) was enough to get it wrongly redirected to login. Fixed with a new `TenantSessionConcurrencyFilter` that wraps the real `ConcurrentSessionFilter` but skips it for this chain's own machine-authenticated SAS endpoints, while deliberately still enforcing it on `/oauth2/authorize` (where redirecting a stale session to login is the correct, intended outcome). `DeleteAccountIntegrationTest`, `DeleteOrganizationIntegrationTest`, and `RefreshTokenRotationIntegrationTest` all green together after both fixes, plus a dedicated unit test for the new filter's own routing decision and a full `app`-module regression run.

**TD-SEC-030** (admin-API rate limiting): `AdminApiSecurityConfig` now wires its own `AntiAbuseRateLimitingFilter`, keyed by the authenticated `client_id` (a new `RateLimitIdentifiers.authenticatedPlatformClientId`), not by IP — every caller on this surface is a backend service, not a browser. A blanket per-client ceiling across the whole surface plus much tighter ceilings specifically on the two `:delete` endpoints, same "defence in depth per rule" shape this file's own scope checks already use.

**TD-ARCH-007** (organization-deletion outbox event): organization-module gained its own `OrganizationDeletedEvent`/`EventOutboxWriter`/JPA persistence trio, deliberately not reusing identity-module's own class names or table name. **Caught a second real risk before it shipped**: a same-named `event_outbox` table would have collided outright with identity-module's own the first time both modules' migrations ran together in the real, shared-Postgres-schema app — confirmed via `application.yml`'s own Flyway-locations comment before naming the new table `organization_event_outbox` instead.

## 14. SDE-III review, 2026-08-27 — whole-codebase pass, and an honest v1 completion estimate

Requested explicitly: a full SDE-III-caliber pass over the current state (not just this week's own diff), technical debt updated with whatever it finds, this roadmap updated if warranted, and a real, unflattering completion percentage — not a vanity number.

**Strengths, verified fresh this session, not carried forward from memory:**
- **97%+ aggregate coverage holds under real load-bearing change.** `app/target/site/jacoco-aggregate/index.html`, recomputed today: 97% instruction, 94% branch, ~98% line coverage across all 5 modules — after a week that included two live-caught regressions (TD-SEC-031) and a same-day cross-module refactor (TD-ARCH-007's own dedup fix), not just quiet weeks.
- **Hexagonal boundary discipline holds under direct inspection, not just convention.** Checked fresh, not assumed: zero `org.springframework`/`jakarta.persistence` imports anywhere under any module's `domain/`; the five hits for `infrastructure.` inside `application/` are all Javadoc prose referencing the concrete adapter for context, not real imports — verified by reading each one, not just counting the grep.
- **Zero `TODO`/`FIXME`/`XXX` anywhere under any module's `src/main`, still true**, now spanning 325 main-source files across 5 modules (up from the 287 CLAUDE.md §11 last counted).
- **The SonarCloud quality gate is genuinely enforced, not decorative.** This week's own PR2/PR3 needed 7 real fix-and-reverify rounds (duplicate literal, duplicate method body, coverage-threshold gap, cross-module duplication, `java:S5778`, AssertJ `isZero()`) before merge — every one investigated and fixed for real (including a genuine architectural extraction into `common`, not a suppression), none waved through. See TD-PROC-009 below for the real cost of *how* that happened, which is a separate finding from *whether* it happened correctly.
- **Branch hygiene, corrected same-day.** A stray typo'd branch (`ecurity/admin-api-rate-limiting`, local and remote) from an earlier `git checkout` sat unnoticed for several days; found and removed in this session's own post-merge cleanup, along with 7 other now-fully-merged branches, confirmed via `git branch --merged origin/master` before deleting any of them, not by assumption.

**Weaknesses, found by looking past what shipped this week, not at it:**
- **TD-PROC-002 has not improved — it has gotten worse.** Checked every doc header in `docs/`, not just the ones this session touched: **0 of 27** carry `✅ Aprobado`; every single one is still `🟡 En revisión`, several unchanged since 2026-08-20 (one week before this review). This row has now been named in at least four consecutive SDE-III passes (this one, and the ones behind §12/§13) without a single doc being promoted. Naming it a fifth time without action would make this review complicit in the same pattern it keeps criticizing — see point 1 below.
- **TD-FUT-013 (production deployment artifact) has been "next" for three consecutive review passes (2026-08-25, 2026-08-26, and now 2026-08-27) with zero code or design progress.** It is not a small row: it blocks TD-FUT-017's own open question (what host, how many cores — the Argon2id concurrency ceiling the load test found), blocks the roadmap's own §2 exit criterion (a real deployed instance for JobSeeker to integrate against), and blocks the mandatory external security review from having a real target to review (a review of `docker-compose.yml`-only local dev is a different, lesser thing than a review of what will actually run in production). Every other open row in this register is smaller in scope than this one.
- **New — TD-PROC-009**: SonarCloud's own duplication/coverage/rule-level findings have no local equivalent — `sonar-maven-plugin` runs only inside `ci.yml`, never as part of a local `mvn verify`. This week's own PR3 needed 4 separate push→CI-finding→fix→push round trips for issues that were real but invisible locally. See `technical-debt-register.md` §2 for the full finding.
- **New — TD-ARCH-008**: `common`'s own scope quietly grew past "utilities/base types" (CLAUDE.md §4) to close TD-ARCH-007's duplication finding — its first `@MappedSuperclass` and first cross-module policy helper, with no ADR or `coding-standards.md` entry saying when that choice beats the alternative (a deliberate, documented per-module mirror, the original TD-ARCH-007 choice). See `technical-debt-register.md` §2 for the full finding.
- **The gap between "capability code shipped" and "v1 actually done" is wider than the §2 table's own Status column shows on its own** — see the completion estimate below; this is the same "a table can be individually correct and still mislead by omission" shape §12 already named for the pre-2026-08-24 version of this same table.

**Points worth implementing next, ordered by how directly each blocks something else already committed:**
1. **Actually promote at least one document to `✅ Aprobado`.** Not "review the docs" as a vague action — pick the single highest-leverage one (`security-architecture.md` is the strongest candidate: it is the primary input to the external security review per its own §9, and TD-PROC-002's own description already names it as the reason this matters). A single real promotion breaks the four-review pattern of naming this without acting on it.
2. **TD-FUT-013** — unchanged advice from §12/§13, now said a third time: this is the next real infrastructure investment, and continuing to name it without scheduling it is itself the risk.
3. **TD-PROC-009** — low effort, real payoff: evaluate SonarCloud's local/preview analysis mode or the SonarLint IDE plugin before the next multi-PR series, so the next round of findings surfaces in the editor, not four CI cycles later.
4. **TD-ARCH-008** — smallest of the four: a short `coding-standards.md` addition naming the actual decision rule ("share via `common` when the duplication is pure structural/persistence boilerplate with zero domain meaning; keep the deliberate per-module mirror when the duplicated code encodes anything a future domain-specific change might need to diverge on").
5. Social login and `Workspace` — still the two v1 capability rows with zero code, unchanged since §12/§13. Unstarted, not forgotten, not this review's own recommended next step (1–4 above block more).

**Completion estimate — two numbers, not one, because they answer different questions and blending them would hide the real gap:**

- **v1 capability-row completion: 80%** (8 of 10 rows in §2's own table fully shipped and live-verified; social login and `Workspace` are the only two at zero). This number describes engineering output and is genuinely strong — coverage, architecture discipline, and security-fix turnaround are all real, verified-fresh strengths above, not aspirational.
- **v1 "done" against its own stated exit criterion (§2 above: a real login→token→`/userinfo` round trip against a *deployed* instance, plus a *completed* external security review with no open critical/high findings): materially lower, honestly closer to 55–60%.** Both halves of that exit criterion are currently at 0% executed, not "in progress" — no deployment artifact exists at all (TD-FUT-013), and the external security review is a *target date* (§5 item 10, "~2026-08-31"), not a booked or completed engagement. CLAUDE.md §6 itself calls the review "non-negotiable, not optional" — treating 80% capability completion as "the project is basically done" would understate exactly the two gates this project's own governing document says matter most. Neither gap is a quality problem — everything shipped is genuinely well-built — both are simply real, unstarted work.

The honest single-sentence version, if one number is required: **engineering execution is well ahead of production readiness** — the code is closer to done than the product is to launchable, and the two remaining v1.1-scale gaps (Workspace, social login) are smaller risks to this roadmap right now than the two zero-progress launch gates (deployment artifact, executed security review) are.

### 2026-08-27 follow-up — Workspace shipped same day, point 5 above closed

Requested explicitly, same day as §14 above: create/add-member/role-change/remove-member, full TDD, real Postgres/Testcontainers verification throughout (`organization-module`: 95 tests; `identity-module`: 264; `client-registry-module`: 45; `app`: 171, including the new `WorkspaceIntegrationTest`'s 7 end-to-end cases — all green, plus a full `mvn clean install` across the reactor). §2's table row updated above; `security-architecture.md` §7b has the full design/verification writeup; ADR-0010 §3 has the addendum recording the v1 scope decision (two-role membership, no invitation — narrower than §14 point 5's own "unstarted" framing implied, since the decision to narrow scope was made *this same day*, not before).

This also closed both of ADR-0007's own long-open cascade obligations that could only be closed once Workspace existed: `WorkspaceMembership` erasure on `Account` hard-delete (a new port/bridge, `security-architecture.md` §7 step 2) and the free DB-level cascade on `Organization` hard-delete (`security-architecture.md` §7a) — both were explicitly named as "revisit the day Workspace ships" in `DeleteAccountService`'s/`DeleteOrganizationService`'s own Javadoc before today, and both are now live-verified, not just designed.

**Revised completion estimate**: v1 capability-row completion is now **90%** (9 of 10 rows in §2's own table shipped and live-verified — social login is the only row remaining at zero). The second number from §14 above is **unchanged** — v1 "done" against its own stated exit criterion is still materially lower (still ~55-60%, both halves of that criterion still at 0% executed) — shipping one more capability row doesn't move either the deployment-artifact or external-security-review needle, and neither should it: those are the two gates that actually gate launch, independent of how many capability rows are done.

### 2026-08-27 second follow-up — Workspace-role login signal + reversible account suspend/ban

Requested explicitly, same day, in response to a real usage scenario the user walked through: a consuming application (JobSeeker) needs to know, at the exact moment a user logs in, whether that user should see its own admin panel (not the Workspace-management-in-Clavaris meaning `BR-WS-05` already covers), plus a reversible way to ban a user without the existing permanent hard delete. Four explicit decisions before any code: the login-time signal is a custom ID-token/`/userinfo` claim (not webhook-only — `webhook-module`/ADR-0007 stays deferred to v1.1, unchanged), the ban is a new reversible `SuspendAccount`/`ReactivateAccount` use case using the already-modeled-but-never-exposed `AccountStatus.SUSPENDED`, and a related idea — self-registration + approval for a consuming application's own public end users (candidates/recruiters) — is a separate, tenant-optional future feature, documented not built (`technical-debt-register.md` §3).

Both shipped same day: BR-WS-06 (`workspace_id`/`workspace_role` claims, `security-architecture.md` §7c) and BR-ID-08 (reversible suspend/reactivate, `security-architecture.md` §7d) — full TDD, real Postgres/Testcontainers verification (`WorkspaceRoleClaimIntegrationTest`, `AccountSuspensionIntegrationTest`, plus unit/persistence coverage at every layer touched), full `app`-module regression run to confirm no existing OIDC/userinfo behavior regressed. A genuine implementation gap was found and closed along the way, not assumed away: Spring Authorization Server's own default `/userinfo` mapper silently drops any custom claim (confirmed via `javap` against the real resolved jar, a live-failing test before the fix, not inspection) — `WorkspaceAwareOidcUserInfoMapper` replaces it, faithfully reproducing the default's own standard-claim filtering so nothing already-conformant regresses.

Neither addition changes the completion estimate above — both are v1.1-tier hardening/UX on top of an already-shipped v1 capability row (Workspace), not a new row in §2's own table.

## 15. SDE-III review, 2026-08-28 — a debt-paydown week's own follow-through, and an updated honest completion estimate

Requested explicitly, same MAANG-caliber-SDE-III framing as §12/§13/§14: strengths and weaknesses of the current state, technical debt updated with whatever is found, this roadmap updated if warranted, concrete points to implement, and an honest, unflattering completion percentage.

**Strengths, verified fresh this session, not carried forward from memory:**
- **Coverage holds, again, after a week that included two more real workflows shipping.** `app/target/site/jacoco-aggregate/index.html`, recomputed today against a fresh `mvn clean install`: 97.9% instruction / 98.3% line coverage, essentially unchanged from §14's own 97%/98% a day earlier despite Workspace, the login-role-claim, reversible suspend, and this week's own technical-debt paydown (retention job, concurrency test, global exception handler) all landing in between. This is real evidence the TDD discipline this project claims for itself is a habit, not a one-time push.
- **Architecture tests are catching real regressions, not just documenting intent.** `HexagonalArchitectureTest` and `OpenApiDocumentationTest` both passed clean against every new endpoint this week (Workspace's 6, suspend/reactivate's 2) without anyone having to remember to check by hand — the second one specifically exists to make "a new `@RestController` method silently missing `@Operation`" a CI failure instead of a documentation gap nobody notices.
- **Two real, would-have-shipped bugs were caught by the existing suite before merge, not after.** `WorkspaceRoleClaimsCustomizer`'s original `AUTHORIZATION_CODE`-only grant-type guard would have silently dropped the `workspace_role` claim on every refresh-token exchange — caught by reading Spring Authorization Server's own source before a test even had to find it, then proven with one anyway. `GlobalExceptionHandler`'s first version (no `ResponseEntityExceptionHandler` supertype) silently turned `CreateOrganizationIntegrationTest`'s own 400-on-invalid-input assertions into 500s — caught by that exact pre-existing test on the very next full build, not discovered later. Both are the kind of defect class `threat-model-stride.md` §5 names as historically easy to miss and hard to catch in review (Clerk's own disclosed CVEs) — here, caught by tests, twice, in the same week.
- **The technical-debt register is a real audit trail, not a wishlist.** 54 rows now closed with a dated resolution note each (`technical-debt-register.md` §6), several this week alone (TD-ARCH-005, TD-TEST-001, TD-SEC-015, TD-PROC-003) — each closure independently verified against a real, fresh `mvn clean install`, not assumed from the diff.
- **A genuinely new load-testing capability shipped on request, with an honest, non-cherry-picked result.** `load-testing/Argon2ConcurrencyBenchmark.java` isolated Argon2id's own concurrency cost from the rest of the stack and reported a real trade-off (helps at moderate concurrency, hurts at severe oversubscription) rather than a story that only supported one conclusion — the kind of finding that's easy to round off in a summary and wasn't.

**Weaknesses, found by looking past what shipped this week, not at it:**
- **TD-PROC-002 is now unchanged for a fifth consecutive SDE-III review** (this one, and the four behind §12/§13/§14 and the original 2026-08-20 finding). **0 of 28** doc headers carry `✅ Aprobado`, including `security-architecture.md` itself, which §14 already named as the single highest-leverage candidate to promote. Checked what's actually still blocking that specific promotion, not just repeated the recommendation: `threat-model-stride.md` §6's own gap list is clean except for one explicitly risk-accepted item (MFA, deferred to v1.1) — nothing found this session disqualifies `security-architecture.md` from promotion on substance. It has now been recommended, not acted on, four times.
- **TD-FUT-013 (production deployment artifact) is now unchanged for a fourth consecutive review**, and the clock on it just changed shape: the external security review's own self-set target (§5 item 10, "~2026-08-31") is 3 calendar days out as of this review, with no evidence anywhere in this repo that a reviewer has been contacted or a slot booked. A precision correction worth naming, not just the repeated flag: `ci.yml`'s `docker-build` job already produces a real, Trivy-scanned, working single-container image on every push — "no production deployment artifact" is accurate about orchestration (zero K8s/Helm/Terraform anywhere) but overstates the gap slightly; what's actually missing is a registry to push that image to and something to run more than one of it.
- **Small, real hygiene gap found and fixed in this same session, not just flagged**: two fully-merged branches (`feat/workspace-membership-`, `chore/oauth2-retention-controller-advice-concurrency-test`) were left on `origin` past their merge — same pattern §14's own "branch hygiene, corrected same-day" finding named once before, now recurring. Deleted both, local and remote, as part of this review rather than only naming it.
- **A small, real doc-precision bug found while checking whether `security-architecture.md` was actually ready to promote**: its own §9 pointed at `threat-model-stride.md` §5 ("Lessons from Clerk's disclosed CVEs," a threat/test table) as the gap list gating the external review, when the real gap list is §6. Fixed in place.
- **TD-ARCH-008 and TD-PROC-009**, both named in §14, remain exactly as open as they were a day ago — neither is large, and neither was in scope for the debt-paydown pass that ran this session (which targeted a different, explicitly-named list), but naming them a second time without action starts to resemble the TD-PROC-002 pattern above.

**Points worth implementing next, ordered by how directly each blocks something else already committed:**
1. **Decide on `security-architecture.md`'s promotion, one way or the other — this review found no substantive blocker, only that nobody has made the call.** Either promote it (the concrete, no-longer-vague action four prior reviews stopped short of) or write down the specific reason it isn't ready yet, so the fifth review doesn't repeat this unchanged a sixth time.
2. **TD-FUT-013 — same advice as §12/§13/§14, now with a real deadline attached**: with 3 days left on the self-set external-review target, either this gets real scheduling movement immediately, or the target date itself needs an honest, documented revision — letting it silently slip past 08-31 unaddressed would be a worse outcome than moving it deliberately.
3. **TD-PROC-009** (SonarCloud local/preview mode) and **TD-ARCH-008** (`common`'s own scope-growth decision rule) — both still cheap, both still real, unchanged advice from §14.
4. Social login — still the one v1 capability row with zero code, still not this review's own recommended next step (1–3 above block more, same reasoning §14 already gave).

**Completion estimate — same two-number split as §14, recomputed, not re-typed:**

- **v1 capability-row completion: unchanged at 90%** (9 of 10 rows in §2's own table shipped and live-verified). Nothing shipped this session added or changed a capability row — this week's own work was entirely hardening/debt-paydown on top of already-shipped rows, exactly as §14 predicted it would be.
- **v1 "done" against its own stated exit criterion: still ~55-60%, unchanged, and now with a visible schedule risk on top of the flat number.** Both halves of the exit criterion (a deployed instance, a completed external security review) remain at 0% executed — a working container image is real progress on the *artifact* half of deployment, but orchestration is still zero, and the review is now closer to overdue against its own target than to booked. The honest read: engineering execution keeps outpacing production readiness by a wider margin each review, not a narrowing one — the code doesn't need this gap to close so much as the two launch gates need someone to start executing against them.

The single-sentence version, unchanged from §14 because nothing this week changed it: **engineering execution is well ahead of production readiness**, and this session's own findings (the doc-approval stagnation, the approaching security-review deadline with no visible booking) are evidence that gap is a real, current risk to name plainly, not a hypothetical one to keep deferring.

### 2026-08-28 follow-up — `security-architecture.md` promoted, TD-FUT-013 shipped same day

Requested explicitly, same day as §15 above: act on the two concrete points that review named, not just re-flag them a sixth time.

**`security-architecture.md` promoted to `✅ Aprobado`** — the first of 28 docs to move (TD-PROC-002). §15's own review already confirmed no substantive blocker remained; this follow-up made the actual decision instead of naming it a sixth time.

**TD-FUT-013 shipped**: a real, TLS-terminated, single-VM production deployment — `docker-compose.prod.yml`, `Caddyfile` (automatic Let's Encrypt), and `ci.yml`'s `docker-build` job extended to push the already-Trivy-scanned image to `ghcr.io/<owner>/clavaris` on every merge to `master`. `docs/05-engineering/deployment-runbook.md` is the real first-deploy/routine-deploy/rollback procedure. Deliberately single-VM, not multi-instance — `TD-FUT-020` (opened same day, `technical-debt-register.md` §3) names the orchestration work as real, anticipated, future debt rather than leaving "why not Kubernetes" implicit.

**Revised completion estimate**: the exit criterion's own deployment half is no longer at 0% — a real deployed instance now exists for a consuming application to integrate against, satisfying that half of §2's own exit criterion. The external security review half is unchanged: still a target date, not a completed engagement, and that target (~2026-08-31) is now imminent. **v1 "done" against its own exit criterion moves to ~70-75%** (up from ~55-60% in §14/§15) — a real, honest jump, not a rounding change: one of the two 0%-executed launch gates is now genuinely done, not just narrowed in scope. The second gate (the security review itself) is the one remaining number at 0% executed, and is now the single largest remaining gap between "capability-complete" (90%) and "launchable."

### 2026-08-28 second follow-up — Infisical removed (ADR-0019)

Requested explicitly, same day: analyze whether Infisical was still worth keeping now that ADR-0018 made `.env` the real production path. It wasn't — **removed entirely, not left dormant** (`docs/03-architecture/adr/0019-remove-infisical.md`): the Infisical CLI was installed *unconditionally* in every image regardless of whether that path was ever used, and was the sole cause of 7 accepted CVEs (TD-SEC-024) this project had to keep tracking for a code path no real deployment has ever exercised. `docker-compose.infisical.yml`, `infisical-setup.md`, the CLI install, `docker-entrypoint.sh`'s dual-mode branch, and every `INFISICAL_*` var across both Compose files and `.env.example` are gone; `ADR-0014` marked Superseded, not edited or deleted.

Directly corrects §13's own "Four Compose files now exist... Infisical, observability, override" finding — that count is now three, and the "operational surface for one person keeps growing" trend §13 flagged as worth watching moved backward for the first time instead of only ever growing. Does not change the completion estimate above — this was hardening/simplification, not a capability or launch-gate change.

## 16. SDE-III review, 2026-08-28 (third pass) — verifying the week's own shipped infrastructure, not just re-describing it

Requested again, same MAANG-caliber framing as §15: strengths/weaknesses, debt updated, roadmap updated if warranted, an honest completion percentage. This pass's own finding is different in kind from §15's — the interesting gap wasn't a stale document, it was an unverified claim sitting inside this week's own freshly-shipped work.

**Strengths, verified fresh, not re-asserted from the prior two sessions today:**
- **Coverage holds exactly steady (97.9% instruction / 98.3% line)** through an entire round of infra-only changes (production deployment, Infisical removal) that touched zero Java source — confirmed via a fresh `mvn clean install`, not assumed unchanged because "nothing Java changed."
- **The full production deployment chain was run for real, end to end, for the first time.** Earlier today's own TD-FUT-013 closure validated `docker-compose.prod.yml`'s syntax and the `Caddyfile` against the real Caddy binary, and `scripts/host/deploy.sh`'s rollback *mechanics* against a disposable dummy service — real verification, but never the actual `deploy.sh` script itself, against the actual published `ghcr.io/redearmy/clavaris:latest` image, against real Postgres/Redis containers, start to finish. Run in this pass: a genuine `./deploy.sh` against that real image pulled the real (Infisical-free) build, brought up all four services, and confirmed `app` reported healthy within the timeout — the first true end-to-end proof this artifact works, not just that its parts individually check out.
- **The Infisical removal's own runtime risk was closed the same way** — `docker build` against the simplified `Dockerfile`/`docker-entrypoint.sh` succeeded, and the resulting image was actually run (`docker run`), confirmed to start without an entrypoint error rather than only trusted from `bash -n` and a read-through.

**Weaknesses found this pass:**
- **Branch hygiene — a third occurrence of the identical pattern**, not a new kind of finding: `chore/production-deployment-tooling`, fully merged (PR #68), was still sitting on `origin` undeleted, same as the branch §14 found and the branch §15 found. Three occurrences in three consecutive review passes is no longer "an occasional miss caught by review" — it's a systemic gap review-and-manual-cleanup doesn't actually fix, since it only ever catches the *previous* branch, one review late. Cleaned again this pass, but the concrete recommendation this time is structural, not another manual sweep: **enable GitHub's "Automatically delete head branches" repository setting** — a one-time, one-click fix that makes this entire finding category permanently stop recurring, something outside this environment's own reach to flip (a repo-settings change, not a code change).
- **TD-PROC-002 essentially unchanged**: 1 of 30 non-ADR docs approved (the doc count itself grew by one — `deployment-runbook.md` — since §15 counted 28). The one real promotion earlier today didn't start a trend yet; still worth naming, not yet worth re-litigating beyond that.

**Points worth implementing next:**
1. **Flip the GitHub branch-auto-delete setting** — the cheapest, highest-leverage item in this pass; closes a finding that has now cost three separate review passes' worth of manual attention.
2. **The external security review** — unchanged advice, now literally the only item left below 100% on the checklist that isn't a deliberately-deferred future-debt row (TD-FUT-020/021, TD-FUT-002). Everything else this project's own engineering can control is done; this one isn't in engineering's control to close alone.
3. TD-PROC-002's remaining 29 docs, TD-ARCH-008, TD-PROC-009 — all still open, all still smaller than item 2, unchanged from §14/§15.

**Completion estimate — unchanged from the two follow-ups above, and that's itself the finding worth stating plainly:**

- **v1 capability-row completion: 90%**, unchanged — no capability row moved this pass.
- **v1 "done" against its own exit criterion: ~70-75%**, unchanged — but now resting on stronger evidence than a moment ago: the deployment half of that criterion is no longer just "an artifact exists," it's "the artifact was actually run, end to end, against the real published image, and it worked." The number didn't move; confidence in the number did.

The external security review is now, cleanly, the one remaining gap between "capability-complete engineering" and "launchable product" — not one of several roughly-equal gaps anymore. Every other item this session's three passes found is either fixed, a deliberately-deferred and named future-debt row, or a repo-settings toggle away from fixed.
