CLAUDE.md
CLAUDE.local.md
.claude/

# Java / Maven
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# Node — app/package.json's own node:test suite for app/src/main/resources/static/js. No
# dependencies (node:test is a Node built-in, no node_modules ever exists here), but the
# coverage/test-run artifacts still shouldn't be committed.
lcov.info
node_modules/

# IDE
.idea/
*.iml
.vscode/

# Env / secrets
.env
.env.*
!.env.example

# TD-SEC-002: local dev-default signing-key store (application.yml's own
# TOKEN_SIGNING_KEY_STORE_PATH fallback) — real generated RSA key material, never committed.
/data/
/app/data/
*.p12

# TD-FUT-011: real Alertmanager SMTP credentials (Resend's own relay) — never committed, same
# "names-only .example template, real file gitignored" convention as .env/.env.example.
/infra/observability/alertmanager.prod.yml

# OS
.DS_Store
